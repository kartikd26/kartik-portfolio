"use client";


/**
 * Single ambient background glow — Apple-style restraint.
 * One warm gradient that slowly shifts position with scroll.
 * That's it. No particles, rings, chromatic aberration, or grain.
 */
export default function AmbientGlow() {
  // Notion uses a flat white canvas — no ambient glows.
  return null;
}
